{
  "name": "oslt-live-translate",
  "version": "1.0.0",
  "description": "Desktop OCR translation overlay that preserves paragraph positions on screen",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:native": "node scripts/build-native-capture.js",
    "benchmark": "node scripts/benchmark.js",
    "check": "node --check main.js && node --check preload.js && node --check lib/style-preserver.js && node --check lib/promise-cache.js && node --check lib/image-signature.js && node --check lib/layout.js && node --check lib/screen-capture.js && node --check scripts/build-native-capture.js && node --check scripts/benchmark.js",
    "test": "npm run check && node --test"
  },
  "keywords": [
    "electron",
    "ocr",
    "overlay",
    "screen-translation",
    "tesseract"
  ],
  "author": "cnd173",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^43.3.0"
  },
  "dependencies": {
    "jimp": "^1.6.1",
    "screenshot-desktop": "^1.15.4",
    "tesseract.js": "^7.0.0"
  }
}
